# Install des deps sans s'appuyer sur package-lock.json (évite le bug npm
# "Cannot read properties of undefined (reading 'extraneous')" dans l'image Nixpacks).
[phases.install]
cmds = ["rm -f package-lock.json", "npm install"]
