# Force npm install instead of npm ci (évite l'échec si package-lock.json
# est absent du contexte de build Coolify).
# Si package-lock.json est présent, npm install l'utilise pour la reproductibilité.
[phases.install]
cmds = ["npm install"]
